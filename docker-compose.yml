version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8000:8000"

  frontend:
    build: .
    command: python manage.py runserver 0.0.0.0:8001
    ports:
      - "8001:8001"
    depends_on:
      - backend